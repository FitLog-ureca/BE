<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.ureca.fitlog.auth.mapper.AuthMapper">

    <!-- 회원가입 -->
    <insert id="insertUser">
        INSERT INTO users (login_id, username, birth_date, password, created_at)
        VALUES (#{loginId}, #{name}, #{birth}, #{password}, NOW());
    </insert>

    <!-- 최근 삽입된 ID -->
    <select id="findLastInsertId" resultType="long">
        SELECT LAST_INSERT_ID();
    </select>
    <!-- 로그인: ID로 유저 조회 -->
    <select id="findByLoginId" parameterType="string" resultType="com.ureca.fitlog.auth.dto.UserInfo">
        SELECT user_id AS userId,
               login_id AS loginId,
               username AS username,
               password AS password
        FROM users
        WHERE login_id = #{loginId};
    </select>

    <select id="findUserIdByLoginId" parameterType="string" resultType="long">
        SELECT user_id
        FROM users
        WHERE login_id = #{loginId}
            LIMIT 1;
    </select>
</mapper>
