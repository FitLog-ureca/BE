<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.ureca.fitlog.mapper.TodoMapper">
    <!--    todo 생성 -->
    <insert id="insertTodo" parameterType="com.ureca.fitlog.dto.TodoRequestDTO">
        INSERT INTO todos (
            user_id, exercise_id, date, sets_target, reps_target, calories_target, is_completed
        )
        VALUES (1, #{exerciseId}, #{date}, #{setsTarget}, #{repsTarget}, #{caloriesTarget}, false);
    </insert>

    <!-- ✅ 새로 추가: 날짜별 투두 조회 -->
    <select id="findTodosByDate" parameterType="java.time.LocalDate"
            resultType="com.ureca.fitlog.dto.TodoResponseDTO$TodoItem">
        SELECT
            t.todo_id AS todoId,
            e.name AS exerciseName,
            t.sets_target AS setsTarget,
            t.reps_target AS repsTarget,
            t.calories_target AS caloriesTarget,
            t.is_completed AS isCompleted
        FROM todos t
                 JOIN exercises e ON t.exercise_id = e.exercise_id
        WHERE t.date = #{date}
        ORDER BY t.todo_id ASC;
    </select>

</mapper>
